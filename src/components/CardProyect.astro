---
interface Props {
    href: string;
    titulo: string;
    descripcion: string;
}
 const {href,titulo,descripcion} = Astro.props;
---
<div class="cardPro">
    <img src={href} alt={titulo}>
    <h2>{titulo}</h2>
    <p>{descripcion}</p>
    
</div>